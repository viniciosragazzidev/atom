import React from "react";
import LoginForm from "./login-form";

const Login = () => {
  return (
    <main className="w-full h-full min-h-screen flex items-center px-2 ">
      <LoginForm />
    </main>
  );
};

export default Login;
